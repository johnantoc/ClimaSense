import { StyleSheet } from "react-native";

const SwipeableRowStyles = StyleSheet.create({
  actionText: {
    color: "white",
    fontSize: 14,
    padding: 10,
    backgroundColor: "transparent",
    fontFamily: "roboto-condensed-regular",
  },
  rightAction: {
    alignItems: "center",
    flex: 1,
    justifyContent: "center",
  },
});

export default SwipeableRowStyles;
